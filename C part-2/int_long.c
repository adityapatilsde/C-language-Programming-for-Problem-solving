#include <stdio.h>

int main() {
    int balance;
    balance = 500;
    long total;
    total = 10000000;
    //declared and initialized value for int and short 
    // now going to print the values using format specifiers and printf statement
    printf("Balance in millions: %d million\n", balance);
    printf("Total Bank Balance: %ld millions\n", total);
    
    
    return 0;
}