
int main() {
    
    int itemsInCart = 5;     // Initial number of items in the cart
    int targetItems = 10;    // Target number of items to reach

    
    
    targetItems += itemsInCart;
    
    
    
    // Check if the total items in the cart are not equal to the target number
    printf("Is the number of items in the cart not equal to the target? %d\n", itemsInCart!=targetItems);

    return 0;
}
