#include <stdio.h> 
int main () {
  int num = 1;
  int limit;
  scanf("%d",&limit);
  while (num <= limit) {
    if (num %3 == 0) {
      num++;
      continue;
    }
    printf(" %d",num);
    num++;
  }
  return 0;
}