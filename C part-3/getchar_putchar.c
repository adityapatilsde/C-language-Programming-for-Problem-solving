#include <stdio.h>

int main() {
    char ch;

    // Prompt the user to input a character
    ch= getchar();
    
    
    // code to read the character
    printf("You entered: ");
    putchar(ch);

    

    return 0;
}