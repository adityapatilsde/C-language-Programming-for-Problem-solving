#include <stdio.h>

int main() {
    int count = 1;

    while (count<=5) {  // Fill in the correct condition
        printf("Count: %d\n", count);
        count++;        // Fill in the missing increment
    }

    return 0;
}